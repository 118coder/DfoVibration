# DfoVibration-V3 (原 SorahkDFO) — 交接文档 (HANDOFF)

> 更新: 2026-09-10 08:00 · 目的: 让下一个对话/会话无需翻历史即可无缝接手
> 配套: `design.md`(设计规范) · `docs/DESIGN.md` · `震动系统开发规范_v20.md`(§7.9 时间哨兵 / §9 高级算法纪律 / §十-十二 ACT 体系) · `docs/ACT全职业特调表_v19.md`(逐职业 ACT 参数审阅表)
>
> **═══ 本会话收官快照 (09-10 上午 · HEAD 未提交 · 541 测试全绿 · exe 已交付 v20.1, md5 6aa6e7db) ═══**
>
> **⚠ v20.1 = 回归修复 (重要)**: v20.0 曾把优先级阶梯/合并降幅泄漏进**经典模式 0**,
> 用户实机反馈 "ACT1 特调手感不如上一版" → 现已全部 gate 到 `legacy_output_mode != 0`;
> **经典模式 0 与 HEAD 行为等价** (金标测试锁定)。详见第 33 条。
> 默认「经典」= 手感与升级前完全一致; S4 特调是**玩家自选**, 不自动改变默认手感。
>
> **S1 输出引擎模式 + 优先级阶梯 + 合并降幅 + UI 渐进披露 (v20.1, 引擎层合并仅 S1 / 三档模式 / 固定阶梯)**
> - **三档输出模式** `config.vibration.legacy_output_mode` (仅 S1; 默认 0 = 经典管线逐字节不变):
>   0 经典 / 1 S4 纯 / 2 **S4+不丢震动**。模式 2 = S4 输出整形 (迟滞死区真静音 + 重映射在后)
>   + 低于死区的能量**后置携带攒厚再出** (`shape_carry_*`), 不丢击。GUI: 高级调校→输出质感→
>   「S1 输出引擎模式 (S4 特调)」三选一; 模式 1/2 下 S4 的死区%/重映射下限%/重映射开关对 S1 解锁。
> - **优先级阶梯 (仅 S4 模式 1/2, 经典模式 0 不走)**: `VibEngine.hard_cut_l/r` 帧内静音位 ——
>   统合衰减/脉冲落地/单帧脉冲归零后, 爆发保底 (`right.max`) 与输出平滑 (`smooth_out`)
>   不得抬回 (S4 路线恒 false = 红线; 经典模式 0 维持 v19.7 行为)。
> - **合并降幅 (仅 S4 模式 1/2, 经典模式 0 维持相乘)**: S1 命中自适应与持续压制取 `min` 只应用一次。
> - **UI 渐进披露**: 高级区「显示专家参数 (低频微调)」开关 (默认关) —— 关时隐藏群怪/连击的
>   低频滑块 (值不变), 高层旋钮始终可见。
> - **S1 输出引擎模式选择器**上移到高级区顶部 (紧随「显示专家参数」), 默认「经典」。
> - **验证**: 535/0; 离屏截图 (S1 模式 2 选中态 + S4 旋钮解锁; 专家参数关/开两态); 规范 §十三 + 方案文档。
> **用户待实测**: 第八节 G3 + H2。**剩余**: 密度折叠 / merge-hitcap 路径合并 / 死控件清理 (见方案文档 §6)。
>
> **═══ 上一会话收官快照 (09-09 夜 → 09-10 早, HEAD = d92d098 · 521 测试全绿 · exe 已交付 07:55) ═══**
>
> **A. 路线与预设体系 (本轮最大改动; 红线: 所有新行为仅 S1 生效, S4 逐字节零改动)**
> - **路线二选一**: S1(ACT1 老方案)/S4+(新方案) 各自独立视图 ——
>   职业预设: S1 只显示 9 个 "X-ACT" 基础职业 / S4 只显示 14 个原版;
>   通用预设: S1 只显示 ACT1 特供 + 7 个 "X-ACT" + 测试版 / S4 只显示原版。
>   切路线即换整套预设; 已应用职业的恢复快照 `find_class` 两表都查。
> - **ACT 职业特调**: **基底 = ACT1 特供** (用户定稿: ACT1 概念最优先), 再按职业理念覆盖 ——
>   力度档 (原上限 30/33/37/41/46 → ACT 58/63/68/75) + 事件阶梯/衰减 +
>   主反馈 (技能型特殊 / 坦克型受击 / DOT 型持续伤害 / 其余命中) +
>   密度·连击·自适应·移动·窗口·脉冲按职业原型重建 + 马达 ACT 语言
>   (命中偏右 / 受击偏左 / 特殊偏右 / DOT 微轻) + **专属算法保留**。
>   构造链: `config::act1_base_params` + `act_build_params_from` + `act_rebuild_advanced`
>   + `act_motor_item_lr` / `act_rank_lr`; 逐职业参数见 `docs/ACT全职业特调表_v19.md`。
> - **ACT1 名册**: 剔除 鬼剑士（女）/圣职者（女）/魔枪士（男）/守护者（女）/外传
>   五个基类 + 神枪手（男）的合金战士 (剑影/枪剑士/弓箭手本表即无) → 9 个 "-ACT"。
> - **ACT1 特供预设**: 应用即开启高级调校 + 勾选两个风暴静音; 内置管理条目自愈
>   (调乱后点应用即回内置值); "保存为"同名条目 (user_modified=true) 则用户版本优先。
>
> **B. 手感算法 (S1 群怪治理栈 + 风暴体系)**
> - 群怪治理: 命中聚合窗 60ms / 命中限频 6 次/秒 (超额记账) / 脉冲落地 25% /
>   持续压制 3 秒后 -30%; **命中风暴抽样 v16** (默认开): 1 秒内纯命中事件 >10 条 →
>   只保留 50% (丢弃不打锚/不记账/不占限频), 停手 400ms 立即恢复刀刀震。
> - **统合衰减期 v17** (可选, 默认关): 风暴期每击旧包络瞬间消亡 + 重新起振 +
>   固定衰减周期 + 到点硬归零; 开启时抽样/限频/聚合记账/Burst 保底让路。
> - **两个风暴静音开关** (v16.6/16.7, 默认不勾): 风暴期静音 怪物异常反馈 (0x04) /
>   评分点系统 (评分族全部), 零痕迹, 停手自动恢复。
> - **11 个高级算法总开关** (默认全开, 关=回旧行为): 一刀多怪合并 / 命中限频 /
>   命中风暴抽样 / 持续降温 / 脉冲落地 / 连击密度自适应 / 命中自适应 / 走位能量 /
>   自定义衰减时长 / 自定义算法窗口 / 静默·反击·脉冲。
> - 狂战士"持续震"归因结案 (两侧日志实证): 剂量 = 命中链 46% + 移动 26% + 特殊 13%
>   + 受击 11.5%; 怪物异常通道当时滑块实值=4 非 0。
>
> **C. 交互 / UI 修复**
> - **向导弹窗必现 BUG 根因**: 三个向导弹窗共用窗口 ID " " → 同帧新弹窗按钮与刚点击的
>   按钮 ID 重合, 一次点击被吃两遍 → 选「我是 DFO 玩家」后版本询问被瞬间跳过。
>   修复 = 唯一窗口 ID (`dfo_ask_window`/`edition_ask_window`/`guide_window`) +
>   `modal_defer` 延迟一帧串接。
> - **快捷卡 S1 职业行**: 自然流式布局 (转职槽排在职业框真实宽度之后, 间距 5px、上移 3px),
>   消除左右重叠; S4 分支零改动。
> - 信息架构重组 (v16.5): 总阀 = 常用全局旋钮; 高级区 = 群怪·连击·移动·输出·算法窗口·静默·预留。
> - 预设下拉过滤下标错位 BUG (三处): 改真实下标回索引 (`visible_preset_entries`)。
>
> **D. 遗留修复**
> - 60 槽滑块自动落盘 (去抖 1s, 重启不丢) / params[39] 冲突结案 (删死常量) /
>   VIB_RING_INVALID 跳变限频 / E-M-F 算法组哨兵守卫 / 0 强度事件零影响 (不打锚不计密度)。
>
> **用户待实测**: 见第八节 ★09-10 清单 G2。
> **下一个 AI 接手指引**:
> 1. 先读本节 + 第八节 G2 + `docs/ACT全职业特调表_v19.md`; 改动铁律见 `震动系统开发规范_v20.md`。
> 2. 用户反馈多为"某职业还震 / 某处位置差几像素"——按职业名或像素值直接调, 改前先复述意图。
> 3. 构建: `bash sync_and_build.sh` → `cd /e/Sorahk-build && cargo test --release` (必须 0 失败)。
> 4. 交付: `taskkill //IM "SorahkDFO-新UI版.exe" //F` → `cp /e/Sorahk-build/target/release/sorahk.exe "E:\网页小工具\DfoVibration V3版本\SorahkDFO-新UI版.exe"` → md5 核对。
> 5. 打包: 在 `E:\网页小工具\DfoVibration V3版本` 下 `python work/_pack_source.py` (仓库跟踪文件 + 运行时 Config.toml, 顶层 DfoVibration-V3/)。
> 6. 界面验证: 构建副本临时改初始页/滚动偏移 + PowerShell PrintWindow 离屏截图 (模式见 `work/offscreen_capture.ps1`; 步骤记录在会话历史)。
>
> **═══ 上一会话收官快照 (09-09 全天马拉松, HEAD = ec3d8dd · 476 测试全绿 · exe 19:59) ═══**
> **主线一 · 群怪爆震彻底治理 (v14.2→v15.3 四层叠加, 全部 GUI 可调大白话)**:
> ① 命中聚合窗 60ms (群怪一刀多事件合并, 一刀一震) ② 命中限频 6 次/秒 (超额记账不丢击)
> ③ 脉冲落地 25% (高负载期尾巴归零, 脉冲间真静音) ④ 持续压制 (打满限速 3 秒自动再降
> 30%, 血之狂暴/狂战士双倍打击降温)。全部仅在 S1 生效, S4 零变化; 用户定性根因 =
> "一刀砍 5 怪 = 5 条错峰命中事件, 瞬间 5 连震"。
> **主线二 · "完全不震"连环案结案**: 绝对频率遗留开启 (保险A/B 已加: 职业切换强制关 +
> 切通用自动停用职业预设×3 路径) + **0 哨兵 bug 家族** (GetTickCount 跨 2^31/开机
> 24.8 天: silence_until/counter_until/hold_until/rhythm_until 四处已修, S4 长开机
> 全静默级 bug 由此发现) + 手柄/注入链/DLL 全部排除。
> **主线三 · 生效性审计 + 可调性最大化**: 全选项审计 (死控件/路线失效/感知压缩三分类);
> 评分通道 thr3 解耦 (评分全灭实锤); S1 重映射下限随 master 缩放 (三闸恢复线性);
> 衰减 0-5ms 单帧脉冲 + ACT1 命中衰减 25ms; 怪物异常反馈独立通道 (默认 1%, 仅 ACT);
> 总闸 L/R 接通; ACT1 马达 L/R 调校; 描述全面大白话化。
> **用户待实测**: 第八节 A2-E2 (群怪治理/衰减/生效性/新通道/回归)。
> **遗留**: 60 槽滑块不自动落盘; 总闸卡 L/R item 1/2/4/5 死; params[39] 冲突 (P1);
> S4 量纲饱和 (红线未动); modal_window 点状 bug (冻结); ownership 竞态 (记录丢失)。
> (明细: 第 26-30 条为本次群怪治理/审计/新通道各轮全记录; 规范新增 §7.9 时间哨兵守卫
> 与 §9 高级算法 GUI 可调纪律, 后续开发强制执行。)

---

## 一、项目是什么

**DfoVibration-V3** (内部标识仍为 Sorahk/SorahkGui, 未改): 基于 [Sorahk](https://github.com/llnut/Sorahk) 的 DNF 游戏工具:
- **连发**: 按键映射 (键盘/鼠标/手柄触发 → 目标键), 多线程涡轮连发
- **震动**: DLL 采集游戏战斗事件 → Xbox 手柄马达, 参数可调
- 技术栈: **Rust + egui 0.33.3 + eframe(glow) + windows-rs**, 单文件 exe

仓库布局 (2026-09-08 拆分为三个独立 git 仓库, 父目录是纯运行时目录):
- `SorahkDFO源码\` — **主程序仓库** (本目录): 全部源码 + docs 全套文档 + UI部件
- `DLL源码\` — **DfoVibration-DLL 仓库**: 采集 DLL 源码 (含 vib_protocol.h ABI 契约)
- `研究文档\` — **逆向研究仓库**: IDA 报告/分析合集/会话记录/00总览(接手必读)
- `SorahkDFO-新UI版.exe` — **最新交付版** (显示名 DfoVibration-V3)
- `Config.toml` / `Vibration.toml` / `JobVibration.toml` — 用户配置 (App 自管理, serde 全字段落盘)

构建目录: `E:\Sorahk-build\` (中文路径 MinGW 链接会失败, 必须用此 ASCII 目录)。
日常快速自测: `cargo build --profile iter` (无 LTO, 增量); **交付一律 `--release`**。

## 二、当前状态 (2026-09-09 02:50)

✅ **430 测试全绿** · HEAD = bea5f11 · exe 已交付 (02:21) · 源码包已刷新
✅ **震动双路线**: S1 ACT1 → 老方案 (老 DLL 事件语义配套: 通道拆分/合成式输出/量纲归一/
   重映射前置/IVL 20/计数只计真实注入+MOVE 排除/评分与移动输出 max 合成), S4+ 新版 → 现行新方案
   (默认, 逐字节不变)。首启三段向导 (DFO 询问→版本询问→使用说明); 设置「DFO 震动功能」下拉切换;
   「?」重跑完整向导。**ACT1 注入链**: 宿主内置 auto_inject (免 Loader, x64→x86 跨位注入,
   DLL 放游戏目录 G:\game\DNF2008_ACT1\Client 即自动注入, 无 DLL 不注入)
✅ **ACT1 特供预设**: 可选预设「ACT1 特供」(默认之下, 仅 S1 路线显示), 参数表按 65535 量纲
   + v20/v21 规范定稿 (命中 70 强/出血 10 极低/评分低/死亡 45 中/后期连击四件套压制/全局 90×75 限幅)
✅ **UI 定稿**: 手柄映射页右栏快捷卡 (连发/映射/震动/职业+版本状态行) · 左侧选项卡带「修改」后缀
   (手柄映射除外) · 显示文案 DNF→DFO · 手柄热点 ABXY/Back/Start 圈内无文字
✅ **潜藏 bug 修复** (18条): avx2 转义 UB+乱码 / 震动导入值上限钳位
⚠ **重大遗留**: params[39] 引擎槽位冲突 (P1, 需 schema 扩容 60→61, 见 18 条③);
   modal_window 点状 bug (冻结, 见 17 条); ownership 竞态 (记录丢失, 见 18 条④)
✅ 设计系统 **v4 "Violet Night"**: 深色 = 深靛夜空紫韵 (bg #0F0F23 / accent #8B5CF6);
   亮色 = **蓝白基调** (bg #F4F5F7 / accent #4F55E8, 用户明确偏好, 勿改紫)
✅ 手柄 SVG 双变体: 深色版黑机身+**白字母**, 亮色版白机身+**深灰字母**;
   **XABY 彩色字母为用户明确要求保留 (不准改!)**; 机身只允许黑白灰
✅ 设计规范: design.md (v3.2 按钮语义层级强制 + v4 配色说明)

### 今日全部成果 (按提交序, 均已验证):
53e4d0d 弹窗收编四窗 (**已回退 by fdc0725, 构建器点状 bug**) · 64fb370 XABY还原 ·
1820f15 main_window(4797行)拆五模块 · e7f927d modal构建器+iter profile ·
6fae285 v4配色 · ed5dbdd 亮色蓝白+手柄纯黑白 · c780794 热点三层重绘 ·
5f41dc2 手柄双主题 · 34d15ab 紫色圆点图标+托盘加固 · 78e1bac 重置真修复(xinput1_3) ·
9b5f1d8 捕获确认/取消 · db1418f Esc绑定 · 1c864f6 白名单页 ·
6930677 DFO引导/?按钮/多DLL · ff30725 托盘隐藏式修复 · 早期: serde配置迁移+卡键P0批次

### 功能清单 (全部已交付): 白名单独立页 / 两段式首启弹窗(DFO询问→使用说明) /
标题栏「?」/ 手柄快速捕获确认取消+Esc绑定 / XInput 多DLL探测+1_3真实重置 /
托盘隐藏式最小化+Win32恢复 / 手柄双主题+热点重绘 / 配置serde全字段落盘+roundtrip

### 遗留 (下一会话按此接):
1. **弹窗收编 (冻结中)**: 前置 = 先查明 modal_window 构建器点状塌缩根因 (见第八节 17 条), 否则不要动
2. **C1 VibrationParams**: ~50 个震动原子收拢为深模块 (触点多, 需独立会话)
3. ~~params[39] 槽位冲突 schema 扩容 (P1)~~ **已结案 (09-10)**: P_DEC_EFFECT 为无读点死常量, 直接删除; 39 号槽唯一语义 = P_MOVE_BOOST_WIN (移动增强窗口), 无需扩容
4. C6 job_presets TOML 数据驱动 / C7 i18n 透传 getter 删除 / 双胞胎弹窗完全合并
5. 待实机验证: 重置蓝牙重连效果 / 国产手柄多DLL适配 / X→托盘→恢复全流程
6. 导入值上限 (已修 18 条②) / avx2 UB (已修 18 条①) / ownership 竞态 (记录丢失, 见 18 条④)
7. ~~60 槽滑块不自动落盘~~ **已修 (09-10, 第 31 条⑤)**: 拖动镜像 config + 主循环去抖 1s 写盘
8. **ACT 预设参数可能还要迭代**: 逐职业 ACT 参数由"ACT1 基底 + 职业理念"推导
   (见 `docs/ACT全职业特调表_v19.md`), 未经逐职业实机打磨。用户反馈"某职业还震/
   太轻/太重"时, 优先调该职业的档位、密度降幅、连击上限、马达偏置, 并同步回该表。
9. **两路线实时参数共用**: S1/S4 共用同一份 Vibration.toml 的 `[vibration]` 实时段
   (预设列表按路线分流)。若后续要求两路线手感完全独立, 备选方案 = ACT 读独立
   `Vibration-ACT.toml` (用户提过, 本轮未做)。
10. **S4 红线**: 本轮所有改动都 gate 在 legacy(S1); 动 S1 代码时务必确认 S4 路径
    逐字节不变 (回归测试含"新方案红线"用例)。

## 三、构建与验证流程 (铁律)

```bash
# 1. 修改源码: E:\网页小工具\DfoVibration V3版本\SorahkDFO源码\src\gui\
# 2. 同步+构建 (中文路径 MinGW 链接失败, 必须走 ASCII 构建目录):
bash "E:\网页小工具\DfoVibration V3版本\SorahkDFO源码\sync_and_build.sh"
# 3. 测试: cd /e/Sorahk-build && cargo test --release --lib   (174 全绿)
# 4. 交付: taskkill //IM "SorahkDFO-新UI版.exe" //F   (用户运行中会锁文件)
#    cp /e/Sorahk-build/target/release/sorahk.exe "E:\网页小工具\DfoVibration V3版本\SorahkDFO-新UI版.exe"
```
⚠ **交付纪律**: 构建/测试失败时**绝对不要 cp**(管道 `| tail` 会掩码退出码, 曾两次把旧包当新包交付)。cp 前用 `grep -ac "新功能标记" exe` 验证二进制含新代码。

### 验证手段 (实测结论)
- **离屏截图 ✓**: 窗口移到 (2600,100) 屏外 → PowerShell PrintWindow(PW_RENDERFULLCONTENT=2) 抓帧 → 完整渲染, 全程不扰用户。工具脚本模式在 HANDOFF 历史里, 按需重写 (用户在用电脑时用此法)
- **交互验证 ✗ 不可后台化**: egui-winit 忽略未聚焦窗口的 PostMessage 鼠标点击; SendInput 真实点击与用户鼠标冲突 → 按钮级验证只能**请用户手测** (给清单) 或前台自动化 (需用户允许)
- **遮挡白屏 (上游 bug)**: 启动即被全屏遮挡的实例永不呈现首帧, 永远白屏; 任务栏点击一次即恢复。与代码无关 (旧版同样复现, 已对照实验证明)。规避: 启动时保证窗口可见, 或让用户点一下

## 四、与用户协作的注意事项

- 用户以**截图 + 红框**方式反馈视觉问题, 常为裁剪缩放图 (坐标不可信), 需自行离屏复测精确坐标
- 用户说「对齐」可能指水平或垂直——**画中线/描框调试** (`painter().line_segment/rect_stroke` 临时插入, 离屏截图目测) 是最高效的定位手段, 用户也认可此法
- 用户在用电脑时: 只做离屏验证, 不要抢焦点/前置窗口/移动他们的鼠标
- 微调类需求 (±1px) 直接改代码里的常量 (如 `class_nudge`), 改完交付让用户看
- **直接 Edit 源文件, 不要写 Python 补丁脚本** (脚本叠加曾导致括号错乱/重复字段/失败仍交付, 教训见坑清单末条)

## 五、踩过的坑 (别再踩, 全部实测)

### egui 布局
- **`ui.horizontal()` 是 18px 条带**: 子 Ui 初始高度 = `interact_size.y` 且顶在内容区顶部, 直接放入的控件以条带中心居中, 嵌套容器又各有基准 → 顶栏曾出现四种垂直中心。**标题栏用 `ui.horizontal_centered()`** (占满整条高度再垂直居中, 官方为此设计); 勿嵌在垂直流中间
- **`main_align` 不作用于水平行的摆放** (0.33): 非 wrap 水平布局组件一律贴边放, `with_main_align(Center)` 只影响按钮/Frame 内部文字对齐。行级居中 = 测内容宽 → `allocate_ui_with_layout(定宽, …)` 让 `top_down(Center)` 父级居中; **间隙要算两截: add_space + item_spacing**
- **ComboBox 内嵌 horizontal 且顶部锚定**: 放进居中的行会下沉 (combo_h−18)/2; 同行两控件垂直错位 ~3.6px。修法: 等高显式槽位包裹 (极简全职业两级 combo 即此写法), 或按 combo.rect 用 `ui.put` 反推 label
- **`horizontal_centered` 是 centered_and_justified**: 会垂直占满剩余空间, 嵌套在垂直流中间会把后续内容挤出视口
- **top_down 父级下 `add_space` 是垂直空距**, 不能用来水平定位
- **hit-test 平局**: 同层两可交互区距离同为 0 → 后注册者胜; "薄者优先"只对不被完全包含的薄条生效。**大拖动区必须先注册(垫底), 按钮后注册**, 反了按钮全灭 (`buttons_on_window` 测试即此语义)

### 渲染
- **裸 `egui::Mesh` 会整帧白屏**: Mesh::default 的 texture_id = Invalid。渐变用分层半透明矩形 (`widgets::paint_top_glow`)
- **遮挡白屏**: 见"验证手段"; keepalive 线程 (500ms request_repaint) 只缓解不根治
- **真粗体**: `strong()` 只是变色。Bold 字体族在 fonts.rs (segoeuib + msyhbd.ttc + phosphor + Segoe UI Symbol/Emoji 回退), 用 `Theme::font_bold()`; 族缺 emoji/符号回退会渲染成 "?"
- **中英混排基线错位**: Latin=内置字体, CJK 回退雅黑, 度量不同 → 相邻不同字号 label 各自居中会错位; 用 `LayoutJob` 单 galley 共享基线
- **egui-phosphor**: 0.11 跟随 egui 0.33 (0.13 锁 0.35); 常量在 `variants::regular`

### 配置/持久化
- **`save_to_file` 是手工 format! 模板**, 不是 serde 序列化 — **新增 Config 字段必须同步模板行 + format 参数**, 否则字段静默丢失 (guide_seen 曾中招); Option 字段用条件行 (None 不写)
- serde(default) 字段: 旧配置缺字段时取默认 ✓; 但手工模板不写 = 每次启动都是默认值
- **ViewportInfo 的 inner_rect/outer_rect 已是逻辑坐标** — 不要再除以 ppp (双重换算曾致位置记忆缩小 25%)
- **OuterPosition 与 InnerSize 同批会竞争**: 位置可能被吞 → 进入极简后 12 帧位置锁存持续校正 (`minimal_pos_latch`)

### 其他
- `strong()` = 变色非加粗; `truncate_chars` 防中文按字节切片 panic; 保存失败静默 (`let _ =`) 注意
- Area 无 fixed_rect; ViewportCommand 是 `BeginResize` 不是 `StartResize`
- **工作流教训**: Edit 后文件状态失效需重 Read; 失败的构建绝不能交付; 多次叠加的行级补丁会自我冲突 — 大改用整体重写

### 2026-09-09 深夜会话新增 (全部实测)
- **TOML 手改两连坑**: ① sed 追加的键落在文件尾多行数组括号内 → "wanted string or table" 解析失败弹错误窗;
  ② 同名键插两次 → duplicate key 解析失败宿主起不来。**正确姿势 = 锚点插入法** (插在已知平键行如
  `guide_seen = ` 之后), 且每次改完 `grep -c` 查重。
- **tasklist 映像名截断 25 字符**: `DfoVibration_OLD_Loader.exe` 显示为 `...Loader.e` — 进程匹配
  contains 必须去掉 `.exe` 后缀, 否则恒判不运行。
- **egui 0.33 合成点击不可行**: `ctx.input_mut` 推的 PointerButton 事件会在下一帧 begin_pass 被丢弃
  (InputState::begin_pass 只从 RawInput 重建 events) —— UI 自动化走"直接执行按钮分支 + 状态落盘 +
  外部轮询截图握手" (参考第八节 22 条向导取证)。
- **Config.toml 强度三闸 (attack/master/max) GUI 从不回写、长期为 0**: 真实参数在内置默认预设 +
  JobVibration.toml。启动兜底已前置到 AppState::new (pristine→默认预设→applied 职业覆盖,
  镜像震动页 INIT_DONE), **勿删**; tests/vibration_params_tests.rs 的镜像断言与之绑定。
- **交付纪律强化实例**: 本轮曾测试 2 失败仍 cp (链式命令掩码) — cp 前必须显式核对
  `PASSED=x FAILED=0`; 失败时先修再交, 已修并重写对应测试。
- **windows crate 特性按需补**: 跨位注入需要 `Win32_System_ProcessStatus / Diagnostics_ToolHelp /
  Diagnostics_Debug` (auto_inject 移植时已加 Cargo.toml)。
- **git-bash 中文引号坑**: 内嵌 heredoc 的中文锚点易失配 (铁律三), 长文本 patch 一律写成 .py 文件再跑。

## 六、架构速览

```
src/gui/
├── theme.rs           设计系统 v3.1 唯一来源: 色板(深浅)/字号/间距/圆角 + 卡片/徽章/按钮构建器
│                        (⚠ 强调色等微调常量: class_nudge 在 main_window.rs 职业槽)
├── widgets.rs         组件库: Icon(Phosphor)/nav_item/keycap/status_dot/paint_top_glow
├── fonts.rs           字体: 默认族(Ubuntu+雅黑回退) + "Bold"族(segoeuib+msyhbd+phosphor+emoji回退)
├── types.rs           Page 枚举 + KeyCaptureMode
├── mod.rs             SorahkGui 状态容器 (含极简/矩形记忆/分段状态字段) + 样式缓存 + run()
├── main_window.rs     App Shell + 连发页 + 预设管理卡 + 振动页 + 极简模式 + 自绘标题栏 + 首次指引
├── gamepad_mapping.rs 手柄页 (SVG 热点标定 — 坐标与 viewBox 绑定, 勿动)
└── *_dialog.rs        6 弹窗 (色板已迁移至 theme)
resources/gamepad.svg  无文字手柄图
design.md              设计规范 (新页面必读, 与 theme.rs 同步维护)
```

**关键状态字段** (SorahkGui): `minimal_mode` / `minimal_window_rect`·`normal_window_rect` (位置记忆) / `minimal_pos_latch` (位置锁存帧数) / `minimal_vib_preset_job` (极简预设来源, 持久化) / `preset_combo_h` / `minimal_job_block_w` (跨帧实测宽) / `page_preset_*` (预设管理卡状态)

**预设应用方法** (闭包内用 _in 显式参数版, 避免 E0502 整体捕获):
`apply_general_vibration_preset_in / disable_job_vibration_preset_in / apply_job_vibration_preset_in` (+ &mut self 包装版)

## 七、遗留事项 (P2, 不影响功能)

1. device_manager/mouse_* 少量旧灰阶字面量、部分对话框圆角 14/10 未归一
2. gamepad 热点色为 SVG 对比度特调 (故意保留, 勿"修复")
3. badge_clickable 以 rect 位作 Id, 重叠徽章会冲突
4. 手柄热点坐标与 SVG viewBox 绑定 (22 135 545 401), 改图即错位
5. 无边框窗口无系统圆角/阴影 (Win11) — 用户知晓
6. 可选: 小窗常驻置顶 / 完整 i18n / wgpu 圆角

## 八、用户手测清单 (最新一轮, 等用户反馈)

**★2026-09-10 凌晨 待实测 (第 31 条交付, 按优先级)**:
F1. **狂战士复测 (核心)**: 开血之狂暴打群怪 → 命中暴增时进入"风暴"只震一部分
    (默认 1 秒内超 10 条命中 → 保留 50%), 停手半秒再打立即恢复刀刀震。
    觉得还是密 → 「群怪不打手」卡调低"风暴期保留 %"; 觉得太稀 → 调高或把
    "一秒内命中超过几下算风暴"调大; 完全不要 → 该滑块调 0。
F2. **风暴静音怪物异常 (v16.6, 可选开关, 默认不勾)**: 「群怪不打手」→"命中风暴
    抽样"小节勾选"风暴期间暂时关闭怪物异常反馈 (出血/中毒跳字)" → 风暴期出血
    跳字应完全不震且不抢节奏, 停手约半秒恢复; 不勾选时行为与之前完全一致。
    注意该小节在高级调校折叠区内, 需先展开并勾"允许高级调校"。
F3. **风暴静音评分点系统 (v16.7, 可选开关, 默认不勾)**: 同小节勾选"风暴期间暂时
    关闭评分点系统" → 风暴期评分点/评分等级脉冲/怪物死亡等评分族事件应完全不震,
    移动走路不受影响; 停手约半秒恢复。
F4. **风暴静音不再被参数掐死 (v16.8)**: 把"一秒内命中超过几下算风暴"保持默认 10,
    两个静音开关勾上 → 打群怪进入风暴后, 出血跳字与评分族事件应完全不震;
    **把"启用命中风暴抽样"取消勾选 (只检测不抽样) 后, 静音仍应生效** (这是
    v16.6/16.7 的 BUG 复现点)。
F5. **高级算法总开关 (v16.8)**: 各小节标题下的复选框取消勾选 → 对应算法回到旧
    行为 (如取消"启用一刀多怪合并"→ 群怪不再合并; 取消"启用命中限频"→ 每秒
    不再限次数); 重新勾选恢复。默认全部勾选。
F6. **ACT1 特供附加默认 (v16.8)**: 应用「ACT1 特供」预设 → 高级调校自动开启,
    两个风暴静音复选框自动勾选。
F7. **三组补齐总开关 (v16.9)**: 各类事件衰减时长 / 算法窗口时长 / 静默·反击·脉冲
    三组标题下也各有一个复选框; 取消勾选 → 前两组回内置默认值, 静默脉冲组回中性
    (不再静默/不再反击加成/不再发脉冲), 测试强度不受影响。
F8. **统合衰减期 (v17, 默认关, 可选)**: 「群怪不打手」→ 命中风暴抽样小节勾选
    "启用统合衰减期" → 风暴期每一击都重新起振 (旧包络瞬间消失), 衰减统一固定,
    到点硬归零; 连续命中应变成"一击接一击的干净持续震动"而非尾巴叠糊。
    觉得节奏太密/太稀 → 调"统合衰减期 ms" (默认 120, 比命中间隔略长最连贯)。
    开启时抽样/限频/聚合记账对该通道让路 (每一击都震)。
G2. **v19 大任务实测**: ① 点标题栏「?」→ 选「我是 DFO 玩家」→ **必现版本询问弹窗**
    (选完 S1/S4 再进使用说明); ② S1 路线 → 全职业预设页基础职业下拉**只列
    9 个 "X-ACT"** (鬼剑士（男）-ACT 等), S4 原职业全部隐藏; 确认
    **无** 鬼剑士（女）/圣职者（女）/魔枪士/守护者/外传, 且神枪手（男）-ACT 无合金战士;
    ③ S4 路线确认职业下拉回到 14 个原版、ACT 条目全部不可见、原手感不变;
    ④ 通用预设下拉 S1 下**只列 ACT 版** (ACT1 特供 + 各 "-ACT" + 测试版),
    S4 下回到原版且 ACT 条目不可见; ⑤ 下拉选非首项, 确认应用的确实是所选预设
    (原下标错位 BUG)。
G3. **S1 输出引擎模式 (v20.0, 第 32 条交付, 核心)**:
    ① **默认经典模式**: 不切模式时手感应与升级前**完全一致** (零迁移)。
    ② 高级调校 → 输出质感 → 「S1 输出引擎模式 (S4 特调)」选 **S4+不丢震动** → 打群怪:
    应有"一击接一击的干净脉冲"且**每一击都能感觉到** (低于死区的轻反馈攒厚再出, 不消失)。
    觉得轻反馈被吞 → 调低「输出低强度抑制 %」; 觉得太软 → 调高「重映射最小输出 %」;
    脉冲被糊 → 把「输出平滑 %」降到 0-20 (模式 2 下平滑会软化脉冲)。
    ③ 选 **S4 纯** 作对照: 低于死区的轻反馈会消失 (这是预期, 用来对比"丢震动")。
    ④ **优先级阶梯**: 脉冲落地/统合衰减期归零后, 右马达不应被爆发保底抬回
    (持续打群怪验证"脉冲间真静音")。
    ⑤ S4 路线回归: 上述三档模式对 S4 无任何影响 (S4 页不显示该选择器, 手感应与升级前一致)。
H2. **专家参数开关 (v20.0)**: 高级调校 → 勾选/取消「显示专家参数 (低频微调)」→
    取消时群怪不打手的「合成保留%/封顶%/补发窗口」「限频窗口」「风暴统计窗口」
    「统合衰减期 ms」「震尾切断%」与连击自适应的「统计时长/恢复窗口/恢复快慢」
    「斜率/补门槛/恢复间隔/保底」应隐藏, 重新勾选后恢复; **隐藏不影响任何数值**
    (已调好的参数不会被重置)。该开关为会话内状态, 重启软件回到默认关 (更干净)。
F9. **预设回归默认 (v18)**: 把 ACT1 特供调乱后, 下拉选它 → 点"应用"应回到内置
    参数 (命中65/特殊55/受击40 等当前值); 列表里的旧快照会在首次进预设页时自动
    重置。若要保留自己的版本, 用"保存为"填同名"ACT1 特供"保存一次 (之后应用
    就用你的版本; 想再回内置需删除该条目让内置重新插入)。
G1. **怪物异常=0 彻底静默**: 出血/中毒跳字强度调 0 后, 出血洪流不得再影响注入
    节奏 (v16.1: 不再刷新注入窗/不计密度)。
H1. **页面布局走查 (v16.5 用户定稿)**: 震动页自上而下 = 控制中心 → 马达区 →
    **全局总阀** (总调整/攻击频率/强度上限/连击增强 + 随机性 + 移动强度/独立开关/L-R)
    → 伤害飘字 → 评分特效 → 评分衰减 → **高级震动调校** (折叠, 需勾"允许高级调校":
    群怪不打手(S1) → 连击与自适应 → 走路质感与走位能量 → 输出质感 → 算法窗口 →
    静默 → 预留槽位)。
I1. **自动落盘**: 拖动任意 60 槽滑块 (含总闸/飘字/曲线等), 松手约 1 秒后重启软件
    → 数值应保持 (旧版必须点"保存当前"才不丢)。
J1. **ACT1 特供预设**: 重新应用一次 (命中 65/特殊 55/受击 40) 感受收敛后的手感;
    不满意可继续用滑块微调 (现在拖动即自动保存)。

**★2026-09-09 晚 待实测 (上一会话第 28-30 条交付, 按优先级)**:
A2. **群怪综合治理 (v14.2~v15.3 叠加)**: 打群怪一刀只震一下 (命中聚合窗 60ms, 可调
    10-200)、每秒最多 6 记 (命中限频, 可调)、打久自动变轻 (持续压制 3 秒后 -30%, 可调)、
    脉冲间真静音 (脉冲落地 25%, 可调) —— 全部在「群怪聚合」组, 标签大白话。
    血之狂暴 (狂战士双倍打击) 持续 3 秒后自动降温, 停手恢复。
B2. **衰减**: B 区衰减滑块可拉到 0 (0-5ms = 单帧脉冲, 命中帧全幅直出下一帧归零);
    ACT1 预设命中衰减已改 25ms (需重新应用预设)。
C2. **生效性验证**: 调 master/attack/max 滑块应有真实梯度 (重映射下限已随 master 缩放);
    评分事件 (击杀点/闪避等) 应恢复震动 (thr3 已与 S1 解耦); S1 下节流/重映射/低强度
    抑制滑块已禁用并注明"仅 S4"。
D2. **新通道/新调校**: 怪物异常反馈 (0x04 出血/中毒, 默认 1%, 独立于装备特效, 仅 ACT);
    总闸卡"全局总调整"行的 L/R 已接通 (左右马达独立微调); ACT1 预设马达 L/R 已调校
    (命中偏右/受击偏左, 需重新应用预设)。
E2. **回归确认**: 单挑手感不应有任何变化 (所有新算法只在群怪高负载期触发); S4 路线
    理论零变化 (含 0 哨兵修复后的长开机机器)。

**★2026-09-09 凌晨待实测 (第 19-27 条交付, 部分已被上述覆盖)**:
A. **ACT1 实测** (核心): S1 路线 + DfoVibration_OLD.dll 放 G:\game\DNF2008_ACT1\Client →
   开软件 (注入器自动拉起) → 开游戏 (自动注入) → 实战验收: ①攻击每段必震 (IVL 20 修复后)
   ②出血/评分明显变轻 ③怪物死亡中等 ④边走边打/评分期间命中不消失 (v13.37 合成)
   ⑤退出软件重进不进震动页也直接有震动 (启动兜底); 有差异 → 发 SorahkDFO_vib.log ([DROP]/[INJ] 行)
B. **90US 回归**: S4+ 路线 (默认) 手感应与升级前完全一致; 累计震动计数含全部事件 (新方案口径未动)
C. **向导**: 首启/「?」重跑: DFO 询问 → 版本询问 → 使用说明; 选 S1 后通用预设列表出现「ACT1 特供」
   (默认之下), 选 S4 则不出现; 设置「客户端版本」下拉切换即时生效
D. **快捷卡**: 手柄映射页空槽态右栏 (连发/映射/震动/职业 + 版本状态行); 点手柄键进入编辑态时暂时让位
E. **预设列表 + 群怪手感 (09-09 晨新增, 本轮核心)**: ① S1 路线预设下拉: 「ACT1 特供」应在
   「默认」之下第二位 (Vibration.toml 里的错位残留会被自动搬移并落盘, 重启软件后仍在);
   ② **群怪实测**: 单挑/慢速连打手感应与之前完全一致 (记账不介入); 持续打群怪时不再"强烈且
   持续的嗡震", 变成节奏感脉冲 (每击都震、单击略厚、高密度期变疏变轻); 差异 → 发 vib.log
   ([CARRY]/[FLUSH]/[INJ] 行, [DROP] ivl 行应绝迹); ③ 开机超 24.8 天的机器上普通攻击命中
   恢复震动 (0 哨兵修复; 旧版此时会"只有技能震")。

① 首次运行指引 (新配置才弹) → 开始使用 → 不再弹
② 极简 100% 小窗 (多次切换验证)
③ 全职业预设: 基础职业/转职两级选择即点即用; **「鬼剑士」「剑魂」同一水平线 + 行居中** (本轮核心修复)
④ 连发映射页预设管理卡: 保存/切换/重命名/删除双确认
⑤ 设置单齿轮; ⑥ 极简/完整来回切换窗口位置各回各位; ⑦ 剑魂 1px 微调合意度
8. v3.5 (2026-09-07): 新增 DFO 玩家引导 — config.dfo_player (serde default true, 老用户不变); 首启弹窗询问「是否 DFO 玩家」→ false 时隐藏「通用震动」「全职业预设」侧边栏入口 + 极简震动段 (状态行显示已关闭), 设置勾选项 t.dfo_vibration_feature 可随时改; 标题栏新增「?」按钮 (show_guide) 随时重开使用说明; 使用说明重写 (4 步快速上手 + DFO 询问面板 + 国产手柄排查指引); XInput 多 DLL 探测 (1_4/1_3/9_1_0/xinput 顺序, 输入与震动同源, 兜底系统导出) — 国产手柄适配
9. 重构 Phase 1-3 (2026-09-07): main_window.rs 4797 行拆为 gui/{shell(留在 main_window),vibration_page,turbo_page,minimal,guide}; theme 新增 modal_window 构建器 (鼠标×2 弹窗已收编, 新弹窗一律用它); Cargo.toml 新增 [profile.iter] (日常迭代 cargo build --profile iter, 交付仍 --release)。后续大步骤方案: C1 把 ~50 个 vibration_* 原子收进 VibrationParams 深模块 (编译器驱动全量改名); C6 job_presets 14 文件 → include_str! TOML 数据驱动 (JobClass 结构不动); C7 i18n 178 个透传 getter 删除。均未做, 按 HANDOFF 此条即可续作。
10. 白名单独立页 (2026-09-08): Page::Whitelist (TABS 第 3 位, Icon::Shield=SHIELD_CHECK, DFO 门控之外恒可见); config.whitelist_enabled (serde default true) + AppState whitelist_enabled AtomicBool (is_process_whitelisted 首查开关, false=全部放行且列表保留); gui/whitelist_page.rs: 状态胶囊总开关 + 添加(防重名忽略大小写)/浏览(rfd 选 exe)/删除, 实时 save+reload_config; 与设置弹窗编辑同一份列表。首启弹窗已拆两段式: render_dfo_ask_window (第 1 弹) → render_guide_window (第 2 弹, 会话内 dfo_ask_answered 防重复)。
11. 图标 + 托盘加固 (2026-09-08): resources/sorahk.ico 换为主题紫 #6C7CFF 圆角方块 (纯 Python 生成 PNG 条目式 ICO, 16-256 七尺寸, 3.2KB); 托盘四条外部建议核实: V2 cbSize 与 NIM_ADD 检查早已存在、NIF_SHOWTIP 本就不在代码中, 仅采纳 restore_main_window (FindWindowW 按视口标题 "DfoVibration-V3 连发与映射工具" → ShowWindowAsync(SW_RESTORE) → SetForegroundWindow, 托盘双击/菜单"显示窗口"/"关于"三入口调用, 之后仍 request_show_window 双保险); sync_and_build.sh 补 resources/build.rs 同步 (此前漏拷导致图标改动不生效)。
12. 托盘最小化真修复 (2026-09-08): 根因 = 旧实现「最小化到托盘」发 Minimized(true) (最小化到任务栏), 而 winit set_minimized(false) 在 Win11 恢复常静默失效 (上游已知问题)。修复 = 改为 ViewportCommand::Visible(false) 真隐藏 (任务栏按钮消失), 恢复 = 托盘侧 Win32 直连 (restore_main_window) + update 轮询 Visible(true)/Minimized(false)/Focus 三连; FindWindowW 失败会写 TRAY_RESTORE 崩溃日志便于排查。端到端验证: SW_HIDE → 托盘同款 Win32 恢复调用 → 窗口可见且内容完整重绘。
13. 手柄图双主题 (2026-09-08): resources/gamepad-light.svg (亮色变体, 仅 <style> 颜色不同, 几何/viewBox 与 dark 版完全一致 — 热点坐标共用); load_gamepad_texture(ctx, dark) 双变体嵌入, 纹理按主题缓存 (SorahkGui.gamepad_texture_dark 标记, 切主题自动重渲染)。改手柄颜色时两个文件都要改, 且只许动 <style>, 不许动几何。
14. 手柄热点重绘 (2026-09-08): 热点从单层半透明圆改为「玻璃底 + 主环 + 中心点」三层结构; 状态语义: 选中=accent 环+柔光放大 / 已配置=类色 2.0 实线环+实心点 / 空槽=类色 1.4 细线+淡点 / 悬停=柔光+150ms 放大 / 捕获=雷达扩散环; 摇杆按下 (StickClick) 改为隐形热点 (不绘制, 保留命中优先); 文字带微投影且主题感知配色。所有绘制在 gamepad_mapping.rs render_gamepad_svg 内, 几何/命中逻辑未动。
15. 重置手柄真修复 (2026-09-08): 根因 = XInputEnable 在 xinput1_4/9_1_0 (Win8+) 是空操作, 多 DLL 探测选中 1_4 后「重置」退化成清缓存。修复 = xinput.rs 新增 xinput13_enable() (惰性加载 xinput1_3.dll 专取其真实 Enable) + handle_xinput_reset() (1_3 切断电源 → 150ms 断电窗口 → 恢复, 期间清缓存+补发 Released); 回归测试: 1_3 可解析 + 重置接线。
16. 手柄快速捕获确认/取消 (2026-09-08): 捕获不再立即写映射 — SorahkGui.quick_gamepad_pending (槽位 id, is_trigger, 输入名) 暂存捕获结果, 槽位面板顶部渲染「⚠ 待确认」条 (✓ 确认应用 / ✕ 取消, 确认才 save+reload_config); 新捕获开始/Esc 自动丢弃待确认; capture 处理函数 (settings_dialog.rs) 只暂存不再直接落盘。
17. 捕获 Esc 键支持 (2026-09-08): 物理 Esc = 取消捕获 (不变); 槽位面板捕获等待态新增「或直接绑定 Esc 键」按钮 → 结果进待确认条 (确认应用才生效)。ESC 键名在 string_to_vk 已支持 ("ESCAPE"|"ESC")。连发编辑行的 TextEdit 本就可手输键名, 此改动补齐了快速捕获 (无文本框) 这条路径。
16b. 亮色主题回调蓝白 + 手柄纯黑白 (2026-09-08 用户偏好): light() 恢复蓝白基调 (bg #F4F5F7 / accent #4F55E8 电光靛蓝) — 深浅两主题各自成立 (dark=Violet Night, light=蓝白); 手柄双 SVG 移除彩色功能键 (st9-12 字母: dark→#F5F5F5, light→#2B2B2B), 亮色机身蓝灰调全部转纯中性灰。规则: 手柄 SVG 只允许黑白灰。
17. **弹窗收编回退 + modal_window 点状 bug 调查记录 (2026-09-08, 未结案)**: 53e4d0d 把设置/设备/关于/关闭四窗迁到 theme::modal_window 后, 四窗全部塌成 ~24px 点状小窗 (用户实测 + 离屏复现均确认), 已 revert (fdc0725) 回内联样板。调查事实 (全部离屏实测, 复现工具 = work/offscreen_capture.ps1: 启动→移屏外(2600,100)→PrintWindow 抓帧): ① 点确认为经由构建器渲染的弹窗本身 (fill 换红点即变红); ② 二分排除法: id 加 `.with("modal_win")` 后缀 / 标题文本 / radius16+stroke NONE+shadow 三件套 —— 逐项加回内联链全部正常渲染, 与构建器逐字等价却一个点一个全尺寸; ③ 唯一未排除变量 = 「闭包经函数参数转发」(builder 内 `.show(ctx, |ui| add_contents)` vs 内联闭包); ④ 海森堡: 给构建器加每帧文件日志后点状消失, 日志显示此时 egui 布局完全健康 (rect 512×499 居中), 即点状 = 布局正常但绘制塌缩, 与首帧 sizing pass / Area state.size 卡死类机制吻合但未定案; ⑤ egui 源码事实: Window::fixed_size 只作用于内部 Resize 容器 (不设 area.default_size), 窗口可见尺寸实取 Resize::end 的 last_content_size; CollapsingState::show_body_unindented 在 openness≤0 时返回 None (内容不渲染); animate_bool 新 id 首调即返回目标值 1.0。⑥ 鼠标×2 弹窗同用此构建器, 自 e7f927d 起可能一直是点状, 无人开过未察觉。结论: 修复需独立会话专攻 (建议方向: 复现时对比两版的 egui Area state.size / sizing pass 时序), 修复前构建器禁用。
18. **潜藏 bug 修复轮 (2026-09-08 晚, 睡眠时段自主执行)**: 按 diagnosing-bugs 纪律「红灯测试→修复→绿灯」闭环:
    ① **tray.rs xml_escape_avx2 双 bug 修复**: 旧实现 32 字节数据块用 `from_utf8_unchecked` 整块 push (跨块汉字切出非法 UTF-8 = UB), 且尾部兜底按 `byte as char` 处理 (非 ASCII 全变 Latin-1 乱码)。修复 = SIMD 块加纯 ASCII 守卫 (`_mm256_movemask_epi8(chunk)` 高位检查, 混入非 ASCII 即断出), 尾部改 `xml_escape_scalar(&s[i..])` 字符级转义 (i 恒为字符边界)。回归测试 `xml_escape_fast_matches_scalar` (cfg avx2, tray.rs 在 **bin** crate 非 lib, 测试要 `cargo test --bin sorahk` 跑): 红灯确认 case2 跨块汉字不一致 → 修复后 8/8 绿 (RUSTFLAGS="-C target-feature=+avx2")。发布构建未开 avx2 = 此前该路径是死代码, 无线上影响。
    ② **震动导入值上限修复 (job_presets/mod.rs)**: parse_job_export / parse_vibration_export 此前只查数组长度不查数值范围, u32::MAX 级数值直送引擎, rank_lr 负数 `as u32` 回绕。新增 `clamp_params` (60 槽逐一按震动页滑块 hi 钳位, 未列出索引按 % 类 100) + `clamp_signed_u32` (±100 二补码), 两个解析器收口。回归测试 4 个 (越界钳位/合法值不误伤/二补码保留/长度校验) 全绿, 全量 420/0。同时修了一个编译错 (i32 引用上 clamp 需 `(*v)`)。
    ③ **新发现 P1 待办 — params[39] 引擎槽位冲突**: vibration.rs `P_DEC_EFFECT=39` (装备特效衰减) 与 `P_MOVE_BOOST_WIN=39` (移动积累增强窗口, v22 新增) 共用同一槽, 震动页两个滑块写同一位, 后动者覆盖前者; 0..59 无空闲位, 正解需 params 60→61 / advanced 41→42 schema 扩容 (牵动导出格式 len==60 校验 / 内置预设 / config 数组), 需专项会话。
    ④ **旧记录核实**: 「ownership 竞态」细节随旧版 HANDOFF 重写丢失 (全历史 blob 检索只剩一句话), 候选位置 = temp_config 字段级合并 / 原子双写, 无红灯判据不动手; state.rs CaptureMode from_str().unwrap() 为假警报 (Err=Infallible 永不失败); clippy 无 correctness 级新发现 (93 条全是 unused import / cast); rawinput from_raw_parts 越界防护已在 (v1.6 继承)。
19. **震动双路线: S1 ACT1 老方案 / S4+ 新方案 (2026-09-08 深夜, 用户拍板)**: 用户实测老宿主 (`E:\LX\DfoVibration_OLD`, 宿主源码 `C:\srchk_src`) 玩新版客户端"高攻群怪持续震"不爽, 要求兼容新老两客户端 —— 新版走新方案, 老版走老方案。实现 (全部老方案语义 gate 在 `engine.legacy`, 新方案代码路径逐字节不变):
    - **选择流**: 首启 DFO 询问选「是」→ 新增第 1.5 弹「你玩的是哪个版本的 DNF?」(guide.rs `render_edition_ask_window`) → 使用说明; 非 DFO 玩家跳过。设置「DFO 震动功能」新增"客户端版本 (震动方案)"切换按钮 (`add_enabled_ui` 门控 dfo_player)。config 新增 `vib_legacy_client` (默认 false=新方案) + `vib_edition_asked`; state 新增 `vib_legacy_client: AtomicBool` (reload_config 同步, 震动线程每轮读 → 切换即时生效); 设置合并清单补 `vib_edition_asked` (主窗口所有)。
    - **老方案语义 (自 `C:\srchk_src` v13.34/35/27/30 移植, 均用户在老客户端实机验证过)**: ① FONT 通道拆分: 0x60(0x20|0x40, 老 DLL stub10 CC tick)→mode1 hold / 纯 0x20 DOT→mode3 衰减脉冲 / 0x04 怪物 DOT→mode4 脉冲 (V3 的 DLL 现不发 0x40, 0x60 通道待 DLL 侧跟进, 纯 0x20 拆分立即可用); ② update() 合成式输出 (hold/rhythm/decay 三分量取 max, inject 不清共存通道); ③ FONT /100 量纲归一 (+Burst /100 +删 dot_active 0.03 保底); ④ finalize 重映射前置+死区只杀真零 (hi=阈值); ⑤ 分通道注入窗 last_font_ch[4] (补丁A); ⑥ vib_log 诊断日志 ([INJ]/[DROP]/[MV] → exe 同目录 SorahkDFO_vib.log, 老方案专属)。
    - **防倒退红线 (移植时保住的 V3 独有优势)**: before()/wrapping_add 回绕安全、VIB_SHM_VERSION 握手、共享内存环校验、send_vibration 多槽扫描 —— 老宿主均无。
    - **战斗事件统计不动** (用户明确要求"战斗事件不要删掉, 这是要记录的"): 计数在环读取层 (push_event 之外逐条 fetch_add), 与引擎路线无关, 两路线口径一致。
    - **回归测试** (vibration.rs `legacy_route_tests`, bin 目标 4 个): 老方案 finalize 救起 25% 轻反馈 (≥28%)/新方案死区仍归零/老方案 inject 不打断 hold/finalize 两路线同构。全量 428/0。弹窗已离屏截图验证。
    - **验收口径**: 新方案 = 90US 玩起来与升级前完全一致 (默认, 理论零变化); 老方案 = 配老 DLL 在 ACT1 客户端复现 v7 宿主手感。⚠ 老方案在 90US 上 = 用户不爽的"持续震"属预期 (那正是老方案语义), 别当 bug 报。
20. **ACT1 注入器整合 (2026-09-08 深夜, 修"S1 路线无震动"缺失环节)**: ACT1 客户端无 DLL 挂载机制, 依赖 32 位注入器 `DfoVibration_OLD_Loader.exe` 常驻轮询 DNF.exe 注入同目录 `DfoVibration_OLD.dll` (老项目 docs/15_loader注入方案设计.md)。此前 V3 交付缺这一环 → S1 路线无事件源。现整合:
    - **文件**: Loader (84KB) + v13.39 稳定版 DLL 已复制进 V3 运行目录 (与主程序同目录; 注入器按自身路径找 DLL, 自包含)。
    - **生命周期** (`src/legacy_injector.rs`, lib+bin 双注册): S1 路线开启 (首启选择/设置切换/带 legacy 配置启动) → 自动拉起注入器 (CREATE_NO_WINDOW 常驻轮询模式); 路线关闭 (reload_config 单一收口) / 宿主正常退出 (main.rs 收尾) → taskkill 收掉, 防孤儿注入器把老 DLL 注进其他 DNF 进程; 已注入的游戏进程不受影响。
    - **快捷卡状态行** (仅 S1 路线可见): 「ACT1 注入器 · 运行中/未运行」一键启停 + 杀软拦截提示。
    - **坑**: ① tasklist 输出映像名截断 25 字符 (`...Loader.e`), contains 匹配必须去掉 `.exe` 后缀; ② 注入器由宿主 spawn, 宿主被 taskkill 强杀时注入器成孤儿 (优雅退出才走 stop) — 快捷卡按钮可手动收; ③ 双份配置键 (重复 sed 插行) 会让 AppConfig 解析失败宿主不启动, 排查时先查 Config.toml 合法性。
    - **E2E 实证**: legacy 配置启动宿主 → 注入器进程出现 (tasklist); 退出收掉; 快捷卡状态行与真实进程一致 (截图)。注意: 注入器轮询按进程名 `DNF.exe` 无架构过滤, 仅在 S1 路线激活期运行, 切 S4 或退出即收 —— 请勿长期手动裸跑注入器。
21. **ACT1 手感彻底对齐 + 三子代理彻查 (2026-09-09 凌晨, 修"攻击震动丢失/计数狂涨/进页才有震动")**: 用户实测 S1 路线手感与老宿主完全不同。三个并行子代理彻查 + vib.log 实证 (199 条 [DROP] ivl=40), 定案四组根因并全部修复:
    - **根因1 (vib.log 铁证) — 40ms 注入窗吞段**: 老项目 v13.35 定案的"极速连段被宿主注入窗合并"原样复现 (V3 的 font_interval=40)。修复 = 老方案强制 P_FONT_IVL≤20ms (引擎读点 min)。
    - **根因2 (移植 bug, 自查发现) — 分通道窗"先打戳后判定"**: 我移植时把 `last_font_ch[ch]=now` 放在了间隔判定之前, 被丢事件把窗口永远前推 = 通道饥饿。修复 = 打戳移回间隔门之后 (与老宿主一致)。
    - **根因3 — v13.37 两处输出合成未移植**: 评分脉冲/移动通道独占马达, 压掉战斗震动。修复 = 老方案下与 FONT 引擎 finalize 输出取 max (评分窗口内命中、边走边打命中均不再静音); 新方案维持独占。
    - **根因4 — "进震动页才有震动"**: AppState::new 从 config.vibration 恢复 params, 但 attack/master/max 在 Config.toml 恒 0 (GUI 不回写), 真实参数在内置默认预设+JobVibration.toml, 加载代码只在震动页首帧跑。修复 = 启动兜底前置到 AppState::new (pristine→内置默认预设→applied 职业参数覆盖, 含 algo/throttle), 震动页 INIT_DONE 保留作迁移。tests/vibration_params_tests.rs 重写为镜像断言。
    - **老方案输出管线参数内置化**: remap_min=20 强制启用 + 死区 15 + IVL≤20, 不随新方案设置漂移 (死区签名加 thr_pct 参数)。
    - **计数口径 (用户要求"战斗事件要记录"且不影响新版本)**: 老方案下 VEV_MOVE 洪流 (6ms 续期) 不灌"累计震动"总数, rank_type_events[11] 板块照常; 新方案维持全量计数。
    - **注入器改为免 Loader 方案 (老项目 v3 定案移植)**: 移植 `auto_inject.rs` (x64→x86 跨位注入: 枚举目标 32 位模块找 kernel32 → 读 PE 导出表解析 LoadLibraryA → CreateRemoteThread), 宿主内置线程 1.5s 轮询; DLL 定位 = 宿主目录优先、其次 DNF.exe 所在目录 → **用户把 DfoVibration_OLD.dll 丢进游戏目录, 启动映射软件即自动注入, 无 DLL 则不注入 (普通连发玩家零影响)**。`legacy_injector.rs` (spawn Loader 方案) 删除; 运行目录的 Loader/DLL 拷贝已清除 (宿主目录的 DLL 会遮蔽游戏目录的)。
    - **验证**: 全量 426/0 (含重写的 2 个启动参数镜像测试 + 5 个老路线测试); 用户实测路径 = S1 路线 + DLL 放 G:\game\DNF2008_ACT1\Client + 开游戏, vib.log 可复核 ([DROP] ivl 应=20)。
    - **修复批共修 8 个编译错** (windows 特性缺失×3、变量遮蔽、类型转换×2、残留孤块、未用参数) —— 全部构建门禁拦截, 未带病交付。
22. **ACT1 调校定稿 + 计数口径完整移植 + 状态行分流 (2026-09-09)**:
    - **计数口径完整移植 (用户: "战斗事件还在不断地增长")**: 此前只排除了 VEV_MOVE, 本轮移植老宿主的 push_event→bool 口径 (`VibEngine.last_injected` 字段: 入口清零/真实注入置位, 11 处注入点覆盖) —— 老方案计数 = 只计真实注入且排除移动洪流; 新方案维持全量。两路线互不影响。
    - **注入器状态行按版本分流** (快捷卡): S1 → 「ACT1 自动注入 · 已启用 (DLL: 主程序目录/游戏目录)」+ 说明; S4+ → 「S4 震动模块自动注入 · 已启用」; 非 DFO 玩家不显示。两态离屏截图验证。
    - **ACT1 特供调校预设** (`vibration.rs act1_legacy_params/rank_type_gain`): 命中 70 (强) / 出血 0x04=12 (极低) / 评分族 20-30+等级 30 (压低) / 怪物死亡 45 (中等) / 曲线 100 线性 / IVL 20。应用点 = 首启选 S1 与设置切到 S1 时写入共享态 (GUI 滑块随后可微调)。未新增独立 algo (复用现有 v31 框架, algo_id=0 = 纯参数调校)。
    - **排障插曲**: 手改 Config.toml 的 sed 追加落在文件尾多行数组括号内 → TOML "wanted string or table" 错误弹窗 (569×385 小窗) —— 手改配置须用锚点插入法 (guide_seen 行后), 勿裸 append。
    - **验证**: 全量 430/0; 两状态行截图核验; exe 已交付 (01:38)。
    - **遗留提醒**: params[39] 槽位冲突 (第八节 21 条) 在老方案下同样存在, ACT1 调校未触碰 39 号槽。
23. **ACT1 特供调校改为可选预设 (2026-09-09, 用户定稿)**: 应用方式从"路线激活自动应用"改为"**预设列表自选**":
    - ACT1 特供注册进内置预设表 (`config.rs default_vibration_presets` 末尾, 名为「ACT1 特供」, 即默认之下);
      60 参数 = act1 调校表 (命中 70/出血 12/评分低/死亡 45/曲线 100/IVL 20)。
    - **显示门控**: 预设列表三处 (通用震动页/极简模式/快捷卡) 挂 `ensure_act1_preset_listed()` (S1 路线时注册进
      config.vibration_presets, 持久一次) + 过滤器 (`legacy || name != "ACT1 特供"`) —— 仅 S1 路线可见。
    - **应用**: 走既有 apply_general_vibration_preset 链 (内置同名优先), `apply_general_vibration_preset_in`
      加内置 ACT1 兜底查找 (预设不必落在用户列表)。此前的自动应用 (guide/settings) 已移除。
    - 验证: 全量 430/0; 两态截图 (S1 列表含 ACT1/S4 隐藏)。下拉展开态建议用户手测一眼。
    - **位置修正 + 参数表 v2 (2026-09-09, 两子代理调研定稿)**: ① 「ACT1 特供」从列表尾移到「默认」之下
      (default_vibration_presets 顺序 + ensure 插入位置找「默认」+1, 老配置兼容); ② 参数表按
      XBOX360 65535 量纲与 v20/v21 规范重定稿: 命中 0x01=70 强主体 / 技能暴击 0x10=60 分层
      (特殊攻击自带 200ms 静默窗防叠加爆震) / 出血 0x04=10 极低 / DOT 20 / 状态 25 / 受击 45;
      **后期连击压制四件套** (借鉴职业算法的密度/倍率封顶机制, 纯参数实现): 密度自适应启用 (thr45/降40/窗500)
      + 自适应早触发 (thr60/降35/floor30) + 连击 cap 150 + slope 60 —— 前期连击低全不触发 (满强度),
      后期连击暴增自动压; **全局限幅** master 90 × 上限 75 (乘区 ≤67.5%); 死槽 1/2/3/7/8 恒 0;
      **p[39] 勘误: 现为移动积累增强窗口 (引擎已弃 P_DEC_EFFECT), 设 1200, 勿再当装备特效衰减调**。
      评分族按 v27 原则压低 (rank_level 30/各事件 20-45/死亡 45)。算法结论: H 叠层(21)只拖尾不压制、
      B/C/E 加脉冲易过震, 均不借鉴 —— 压制走密度+自适应+倍率封顶三件套 (纯参数, 0 引擎改动)。
24. **预设位置修复 + 群怪聚合 v14.1 参数化 + 0 哨兵修复 (2026-09-09 晨, 终态)**:
    - **预设位置 (已修复保留)**: 根因 = 旧 exe (4249840) 的 ensure 是 **push 到末尾**且已把该条
      持久化进 Vibration.toml 末尾 (第 9 位, v1 参数); bea5f11 只改插入点, "不存在才插"对已存在
      错位条目永不搬动。⚠ 预设列表持久化在 **Vibration.toml** (`[[vibration_presets]]`),
      Config.toml 的 `[[presets]]` 是键位映射, 别看错文件。修复 = 纯函数
      `config::ensure_act1_preset_position(presets, legacy)` (缺失→插「默认」+1 / 错位→搬移 /
      非 S1 不动), `ensure_act1_preset_listed` 变更即落盘自愈。5 测试红灯→绿灯。
    - **0 哨兵修复 (用户确认实锤)**: 用户回退版复测仍"没震动" → 确认 = 开机时长 bug:
      `before(now, deadline) = deadline.wrapping_sub(now) as i32 > 0` 的 0=unset 哨兵在
      GetTickCount 跨 2^31 (开机 **24.8 天**) 后 `0.wrapping_sub(now) as i32` 翻正 →
      `silence_until=0` (从未放技能) 时**普通攻击全部被特殊攻击静默门吞掉 (只有技能震)**,
      `counter_until=0` 时恒入 Counter 态。修复 = 两处 `!= 0` 守卫。**规范第七节新增第 9 条**
      (时间比较守卫纪律) 防复发。本机 uptime 25.4 天实测: 旧版静默, 新版正常。
    - **群怪聚合 v14.1 参数化 (按用户定调重做上车)**: 首版 (012fa6a) 内置常量被回退
      (1c29839) 后, 按新纪律重做 —— **参数全部进 GUI**:
      * 参数链路 (仿 throttle 先例, 独立 config 字段不占 60 槽位): `config.vibration.merge_keep/
        merge_cap/merge_hold` (serde default 80/100/80) → `AppState.vibration_merge_*` 原子
        (state.rs) → run 循环每帧同步引擎 `engine.merge_*` → 生效。
      * **GUI**: 震动页高级区、节流滑块组之后, 「群怪聚合 (S1 高级算法)」三滑块, 仅
        `vib_legacy_client` (S1) 显示: 合并保留 % (0=关闭聚合) / 合并封顶 % (20-150) /
        补发窗口 ms (0=不补发), 拖动即存 Vibration.toml。
      * 引擎语义: 窗内事件不再丢弃 → 按通道记账 (KEEP 边际递减/CAP 封顶×density_scale),
        下一发兑现"单击变厚"; 记账 HOLD ms 到期无注入 → tick max 合成补发收尾脉冲;
        DOT/状态/特效计入密度计数 (v22.4 盲区); Burst 保底×density_scale; legacy 绕过 throttle
        硬限流。**keep=0 = 逐字回到丢弃语义 (+[DROP] ivl 日志回归)**; S4+ 路径逐字节不动
        (font_ch 恒 None 天然不参与)。
      * 测试: 聚合 7 个 (携带/封顶/**keep=0 关闭开关**/hold=0 不补发/DOT 密度/Burst 疲劳/
        新方案红线) + roundtrip 夹具新增 3 字段 (91/92/93)。
    - **新纪律入规范**: `震动系统开发规范_v20.md` 新增第九节「高级算法新增纪律」(GUI 可调
      硬要求/独立 config 链路/默认关闭/双态回归/vib.log 诊断行/文档同步) —— 用户要求:
      **以后增加高级算法和调整都必须照此执行**。
    - 验证: 全量 **454/0** (lib 195 + bin 220 + 集成); 字节验证 [CARRY]/群怪聚合/合并保留
      在交付 exe 内; exe 已交付 (05:11)。
25. **"完全不震"结案 + 双保险 + 命中限频 v14.2 + [GATE] 诊断 (2026-09-09 晨)**:
    - **根因结案 (用户实测确认)**: 「绝对震动频率」(abs_freq_enabled) 被职业预设应用遗留开启
      → 全事件 3s/6 次节流 → "完全不震"。关闭即恢复。v7 老宿主"也不震"是它自己的参数被清零
      (召唤师 font_attack=0 + 主参数 0..18 全 0), 对照组无效; v7 无 0 哨兵 bug (裸比较)。
      注入链/DLL/手柄硬件全部排除 (游戏目录 OLD.dll 采集到 05:49)。
    - **保险A — 职业预设强制关绝对频率**: `apply_job_vibration_preset_in` (vibration_page.rs)
      由 `store(cls.abs_freq_enabled)` 改为强制 false + 立即 save_vibration_to_file;
      两处重启复活路径同步封堵 (vibration_page INIT_DONE 恢复段 + state.rs 启动兜底)。
      **该模式实际退役** (应用/恢复均强制关, 召唤专属复选框只在已开时渲染=不可达);
      需要时手改 Vibration.toml。
    - **保险B — 切通用预设自动停用职业预设**: 快捷卡 (gamepad_mapping.rs) 与极简 (minimal.rs)
      的「通用预设」分段点击处加 `if self.vib_job_enabled { disable_job_vibration_preset(); }`;
      震动页「应用」按钮 (手工拷参路径, 原本绕过 apply_general_preset_in 的自动 disable 链)
      也补 disable —— 职业参数的隐藏复活口全部封堵。disable 参数回滚为内置"默认"预设。
    - **命中限频 v14.2 (仅普通命中通道 ch=3)**: 用户需求"XX 秒内大量命中只生效 XX 次,
      但绝不允许任何普攻没震动"。实现 = IVL 门后、打戳前的翻滚窗限频 (hitcap_max/
      hitcap_win_ms, throttle 同款滚动): 超限命中**全额记账**进 font_carry[3] (不打戳不注入,
      防 IVL 锚点前推=防通道饥饿), 下一发兑现变厚 / merge_hold 到期补发收尾 —— 频率上限
      转化为厚度, 每击必震。计数只在真实注入 +1 (s=0 空枪不占名额, max=0 完全休眠)。
      参数链仿 merge 三件套 (config.vibration.hitcap_max 默认 6 / hitcap_win_ms 默认 1000 →
      AppState 原子 → run 同步 → 引擎); GUI 在「群怪聚合」组内两个滑块 (0=关闭);
      [HITCAP] 日志。新方案 font_ch 恒 None 天然不参与; 密度激活期兑现封顶×density_scale
      联动降温。与 throttle (legacy 已绕过)/密度自适应 (调幅)/v31 算法窗正交无冲突。
    - **[GATE] 输出闸门诊断行**: run 循环内有效输出状态 (总开关/暂停/绝对频率) 翻转即记
      vib.log —— 修"事件消费在总开关之前, 关开关时 [INJ] 照打但马达不动"的排查盲区
      (本次"完全不震"复盘的副产品)。
    - 验证: 全量 **458/0** (lib 198 + bin 223 + 集成; 新增 hitcap 限频记账/关闭态/
      新方案红线 3 测试 + roundtrip 夹具 hitcap 字段); 字节验证 [HITCAP]/命中限频 在交付
      exe 内; exe 已交付 (07:09)。**用户待实测**: 群怪手感 (限频默认 6 次/秒)、
      转职后绝对频率确认为关、主页切通用后职业开关确认为关。
26. **脉冲落地 v15 + 怪物异常反馈独立通道 + tick 0 哨兵修复 (2026-09-09 午)**:
    - **S4/S1 输出差异定调 (先对比后动手, 用户确认)**: S4 群怪手感更优 = 量纲饱和
      (min(1.0) 满幅封顶, 怪再多不更响) + 快衰减 + 28% 迟滞死区真静音 + 全局 40ms 窗;
      S1 的线性量纲/合成式共存/20% 抬底在群怪负载下反转为爆震源。结论: S1 靠
      v14.2 限频 + 本次"脉冲落地"向 S4 质感靠拢; S4 保持零变化。
    - **脉冲落地 v15 (仅 S1)**: 高负载期 (density_active 或 hitcap 咬合 count≥max) 衰减
      尾巴降到**本脉冲峰值**的 tail_land_pct% 即归零 → 脉冲间真静音。实现: inject/inject_hold/
      inject_rhythm/FLUSH 四处记录 tail_peak (FLUSH 不刷新会把小收尾脉冲当帧误杀 —
      有回归测试), 落地门插在 tick legacy 合成块衰减分量之后、max 合成之前 (只清衰减
      分量, hold/rhythm 不受影响)。参数: config.vibration.tail_land_pct (默认 25,
      **0=关闭**), GUI「群怪聚合」组"脉冲落地阈值 %"滑块 (0-60)。engine new() 默认 0
      (run 循环同步覆盖)。
    - **怪物异常反馈独立通道 (仅 S1, 用户定稿"ACT 版本限定")**: 0x04 通道在 ACT 里
      100% 是怪物出血/中毒跳字 (DLL 取证: hooks_old.c stub8 L319-326 怪物侧 DOT 唯一
      出口改道 0x04/v13.29; 游戏原生 0x04=回复数字已改发 0x08; 装备特效从不产生伤害
      数字 → 老宿主从未采集过)。实现: font_pick legacy is_effect 分支改用
      `monster_abnormal` (独立强度, 默认 1%), S4 分支维持 p[15]; GUI 事件类型细分组:
      S1 隐藏"装备特效反馈 (0x04)"行 + 新增"怪物异常反馈 (0x04) 出血/中毒/感电跳字"
      行 (独立 config 字段 monster_abnormal_gain + item_lr[8] 权重沿用); S4 两不变。
      ⚠ 文案: 旧 hint "装备触发效果(特效等), 例如天域之类的" 是历史误称。
      ⚠ 重映射 20% 抬底: 强度 1 实感约 20% 满幅 (UI 已注明), 0=完全关闭。
    - **tick 0 哨兵修复 (写测试时本机复现, S4 全静默级)**: tick 两分支的
      `before(now, self.hold_until)` / `before(now, self.rhythm_until)` 裸调用 ——
      hold_until/rhythm_until=0 (inject 后/初始) 在 GetTickCount 跨 2^31 机器上恒真
      → **S4 分支每帧用 hold_l=0 覆盖输出 = S4 FONT 全静默** (legacy 是 max 合成
      无感)。修复 = 两分支四处加 `!= 0` 守卫。至此 before() 哨兵家族已修:
      silence_until/counter_until (25 条) + hold_until/rhythm_until (本条)。
    - 验证: 全量 **468/0** (lib 203 + bin 228 + 集成; 新增落地密度激活/关闭对照/
      FLUSH 防误杀/S4 红线/怪物异常拆分 5 测试 + roundtrip 夹具 2 字段); 字节验证
      脉冲落地阈值/怪物异常反馈 在交付 exe 内; exe 已交付 (09:01)。
      **用户待实测**: ① S1 群怪 (命中限频 6/s + 落地 25% 应有"脉冲-静音"节奏感);
      ② 单挑不变; ③ 出血/中毒跳字应极轻 (怪物异常反馈 1%), 装备特效滑块在 S1 不再
      出现 (S4 不变); ④ S4 若在本机测, FONT 震动应恢复 (0 哨兵修复)。
27. **全选项生效性审计 + P0 修复 (2026-09-09 午后, 回应"调了没调一样")**:
    - **审计方法**: 子代理全量清点每个 GUI 选项的 数据载体→GUI 写点→引擎消费点 链路,
      按三分类: 链路断裂 / 路线条件失效 / 感知压缩。
    - **链路断裂 (死控件, 本轮标注/部分处理)**: ① params[5]"衰减时间"从无消费点
      (死槽 ★新发现, GUI 有滑块) → hint 已改"(预留槽位未接入)"; ② 总闸卡 L/R 权重
      item 0/1/2/4/5 五对死控件 (引擎只消费 item 3 衰减缩放与 item 6-12 飘字) — 未动;
      ③ p[39] "装备特效衰减 ms"滑块 (真实语义=移动增强窗, 双绑定) → 已从 B 区删除;
      ④ 衰减速度滑块浮点截断 (4.5→4) → 改整数步进。
    - **路线条件失效 (S1 被绕过/覆盖)**: remap_min/remap_enabled (S1 硬编码)、
      out_threshold (S1 FONT 硬编码 15)、throttle 三滑块 (S1 绕过) → UI 已按路线
      禁用并注明"仅 S4"; **★实锤事故: out_threshold=60 经评分通道 thr3 泄漏,
      用户配置下 (rank_level_gain=30 → 脉冲 20%<50% 门) 整个评分通道全灭** →
      thr3 已与 S1 解耦 (legacy 固定 15% 只杀真零)。
    - **感知压缩 (最大元凶, 已修)**: S1 硬重映射 [20%,100%] 把 master/attack/max
      三闸可动范围压到 10-13 个百分点 (master 90→100 仅 +1.5 点)。修复 =
      **抬底下限跟随全局总调整**: mn = 20%×(master/100), scale = 1-mn —— master=90
      时 18% 与原手感几乎一致, 调低则地板同步下沉恢复线性。master=0 仍由早退拦住。
    - **感知压缩放大器 (未动, 记录)**: out_smooth=35 低通削短脉冲峰值 15-45%;
      random_gain=20 抖动与灵敏度同量级; 60 槽滑块改后需点「保存当前」才落盘
      (重启回滚 —— 高频痛点, 建议后续做自动落盘)。
    - **60 槽死槽清单**: 1/2/3/5/7/8 (5 为新发现); 39 双绑定 (P1 遗留)。
    - **S4 饱和影响范围 (红线未动)**: FONT 8 滑块 >~4 全饱和; Burst 保底
      bm×p[16] 未归一 (p43 滑块 S4 实际无效); ghost/密度对 S4 FONT 幅度无效。
    - 其他: ACT1 预设 p[35] 60→25ms (用户"命中后立刻衰减/一顿一顿"); 怪物异常行
      移入事件类型细分网格 (插在装备特效行之下, 装备特效行恢复显示+ACT 注记,
      未删除任何选项)。
    - 验证: 全量 **468/0**; 字节验证 预留槽位/仅S4/脉冲落地阈值; exe 已交付 (15:24)。
      **用户待实测**: 调 master/attack/max 滑块应有真实梯度; 评分事件 (击杀点/闪避
      等) 应恢复震动; 重新应用 ACT1 特供预设以获得 25ms 命中衰减。
28. **衰减 0ms/单帧脉冲 + 总闸 L/R 接通 (2026-09-09 傍晚, 用户"继续优化")**:
    - **衰减支持 0ms (单帧脉冲模式)**: B 区四个衰减滑块下限 15/10 → **0**; 引擎
      decay ≤5ms 走"单帧脉冲": inject/FLUSH 布防 (instant_armed) → 当帧 tick 保持
      全幅直出 → 下一帧硬归零 = "命中后立刻衰减"的最极端形态。6ms 起维持指数衰减。
      修复前衰减常数被 .max(10/.max(30) 钳死且首帧即衰减 60-80% (tick 在 push 之后),
      小衰减值形同虚设。踩坑记录: 落地门条件初版写成 `decay_l > 0.0 && <= 5.0`,
      把 0ms 恰好排除 (测试抓到, 改 `<= 5.0`)。
    - **总闸 L/R 接通 (item_lr[0]/[1])**: 原为死控件 (引擎零消费)。接通为左右马达
      独立总闸微调: finalize `l_raw ×= gate_lr_l` / `r_raw ×= gate_lr_r` (±100% →
      ×0..×2, 与 master 叠乘); run 循环从 vibration_item_lr[0]/[1] 原子同步
      engine.gate_lr_l/r。GUI 无需新增 (总闸卡"全局总调整"行的 L/R 即此)。
    - 其余死端口评估结论 (未接, 避免伪语义): item 1/2/4/5 的 L/R 无自然语义
      (攻击频率/上限/连击是全局系数, 强拆 per-motor 意义牵强); 死槽 1/2/3/7/8
      无 GUI 绑定。如需更多可调项, 优先做"60 槽自动落盘"与 p[39] 槽位扩容 (P1)。
    - 验证: 全量 **470/0** (lib 205 + bin 230 + 集成; 新增单帧脉冲直出/归零 +
      总闸 L/R 独立缩放 2 测试); 字节验证 单帧脉冲 在交付 exe 内; exe 已交付 (16:02)。
      **用户待实测**: B 区衰减拉到 0-5 感受单帧点击感 (最脆); 总闸卡"全局总调整"行
      的 L/R 现在可独立压左/抬右。
29. **命中聚合窗 v15.2 + ACT1 马达 L/R 调校 (2026-09-09 晚, 用户定性群怪根因)**:
    - **用户观察 (群怪爆震真正机制)**: "攻击群怪命中 1 次 = 命中事件 5 次 = 瞬间震
      5 下; 攻速高 → 命中几次但震动几十下"。即群怪时**每怪一条命中事件**且错峰
      到达 (25ms+ 间隔), 20ms 旧聚合窗拦不住 → 瞬间多连震。
    - **命中聚合窗 (hitmerge_ms, 仅 S1 ch=3)**: 普通命中通道专属更宽聚合窗
      (默认 40ms, IVL 计算处 `hitmerge > ivl 则覆盖`), 窗内事件全额记账合并
      (能量不丢) → **一刀砍中几个怪都只震一下 (更厚)**; 单挑慢速攻击 (>40ms)
      完全不受影响; 密度激活期 ivl 翻倍仍可叠加其上。GUI「群怪聚合」组
      "命中聚合窗 ms" 滑块 (10-200, 调大合并更彻底, 10=回到旧行为)。
      参数链: config.vibration.hitmerge_ms (serde default 40) → state 原子 →
      run 同步 engine.hitmerge_ms。
    - **ACT1 特供马达 L/R 调校 (用户: "只改了参数没调马达参数")**: item_lr 从
      [0;26] 改为定制: 命中 (item 11) L-10/R+10 偏右清脆; 受击 (item 12)
      L+15/R-10 左马达低吼; 玩家 DOT (item 7) L-5/R-5 微轻; 特殊 (item 10)
      R+15; 怪物异常 (item 8) 中性 (独立 1% 通道)。rank_lr: 移动 (event 11)
      L-15/R+10 走路低频感, 其余中性。**需重新应用 ACT1 特供预设生效**。
    - 测试坑 (记录): 聚合窗测试的时间戳必须"每次注入前按目标间隔拨锚"
      (`now_ms()-30`), 不能链式从首帧偏移 (真实时钟会流逝, gap 变 970ms)。
    - 验证: 全量 **474/0** (lib 206 + bin 231 + 集成; 新增聚合窗合并/兑现/关闭
      对照/更厚断言); 字节验证 命中聚合窗 在交付 exe 内; exe 已交付 (16:49)。
      **用户待实测**: ① 群怪一刀 = 一下更厚的震 (默认 40ms); ② 重新应用 ACT1
      特供预设感受马达 L/R (命中偏右/受击偏左); ③ 觉得合并不够就把聚合窗调大。
30. **持续压制 v15.3 + 描述大白话化 (2026-09-09 晚, 用户反馈"聚合窗还得调大 +
    狂战士血之狂暴双倍打击仍然很震")**:
    - **聚合窗默认 40→60ms** (用户"聚合怪还得调大点")。
    - **持续压制算法 (仅 S1 ch=3)**: 场景 = 狂战士血之狂暴普攻变 2 段 + 攻速翻倍,
      命中限频虽封顶 6/s 但每发都是满幅合并脉冲 → 仍感"很震"。机制 = 长窗
      (sustain_secs×1000) 统计真实命中注入次数, 达到 **限速上限 × 秒数** (持续
      打满限速) 后, 命中震动自动 ×(1-sustain_reduce%) (默认 3 秒后 -30%);
      降速/停手 → 长窗滚动 → 计数跌破阈值 → 自动恢复。参数: config.vibration.
      sustain_secs (默认 3, **0=关闭**) / sustain_reduce (默认 30) → state 原子 →
      run 同步 → GUI「群怪聚合」组两滑块 ("打群怪几秒后自动降温 (秒, 0=关闭)" /
      "自动降温幅度 %")。设计演变记录: 首版"咬合计时"语义有缺陷 (hitcap 窗滚动
      会重置咬合状态, secs>win 永不触发) → 改为长窗注入计数 (无此问题)。
    - **描述大白话化 (用户要求所有调校描述可直读)**: 群怪聚合组全部标签/hover
      ("群怪不打手"、"合成一下时保留多少力度"、"每秒最多震几下 (0=不限制)"、
      "打群怪几秒后自动降温"等); A4 密度自适应 6 行 ("多快算太密"/"太密时减轻
      多少 %"/"最轻不低于原来的 %"等); D 区命中自适应 7 行 ("打多快算太快"/
      "打太快时减轻多少 %"等); (总闸卡/B/C 区此前已白话)。
    - 验证: 全量 **476/0** (lib 207 + bin 232 + 集成; 新增持续压制 满载降档/
      满强度对照/恢复 3 段测试 + roundtrip 夹具 2 字段); 字节验证 自动降温 在
      交付 exe 内; exe 已交付 (19:59)。
      **用户待实测**: ① 狂战士开血之狂暴打群怪: 3 秒后命中自动变轻 30%;
      ② 普通角色单挑无感 (永不触发); ③ 觉得降得不够 → 调"自动降温幅度"。
      **测试坑 (记录)**: 事件时间戳必须"注入前按目标间隔拨锚"; 拨限频/持续压制
      窗口起点时, 两套窗口 (hitcap_start/sus_start) 要分别拨, 否则一个事件先被
      限频吞掉根本到不了压制层。
31. **命中风暴抽样 v16 + 狂战士归因 + 信息架构重组 v16.3 + 遗留修复 (2026-09-10 凌晨,
    用户三指令一轮完成: "组合起来井然有序 + 参数调整 + 命中暴增只读一部分震动")**:
    - **狂战士"持续震"日志归因 (三路子代理并行取证, 数据先于假设)**: ① 怪物异常
      通道当时滑块实值=4 非 0 (W19 20:11-20:15 共 300 条 [INJ] a6=4 s_out=0.040);
      调 0 后的 W20 时段 0x04 零流量零 [DROP] —— 该通道不是"调 0 仍震"的原因。
      ② 剂量归因: 命中链 35%+FLUSH 11% (限频超额→记账→CARRY/FLUSH 在攻击间隙
      补发) / **移动通道 26%** (0.1 幅度常驻正弦、63% 秒覆盖、最长连续 86s、
      不走 [INJ] 日志 = 排查盲区) / 特殊 13% / 受击 11.5%。③ 血之狂暴自掉血
      已被 DLL 按指纹过滤 (不震)。④ 配置漂移警告: 20:31 落盘的 Vibration.toml
      不是当晚全程参数快照 (rank11 文件 40 vs 日志 25 = 当时应用的是剑魂职业预设)。
    - **命中风暴抽样 v16 (用户定稿算法, S1 默认开启, 规范 §9 例外)**: 1 秒窗
      (storm_win_ms) 内**纯命中事件** (0x01; 受击 0x02/DOT/状态/特效/特殊全部
      排除) 超过 storm_thr=10 条 → 进入风暴, 确定性抽样 (每 N 条保留 1 条,
      N=100/storm_keep_pct, 默认 50%) —— **被丢弃的事件整条跳过: 不打 IVL 锚点/
      不进聚合记账/不占限频名额** (与 merge/hitcap 的"记账不丢击"互补: 风暴期宁可
      少震不要节拍堆叠); 距上一条纯命中超过 storm_pause_ms=400 → 立即退出风暴,
      恢复刀刀震动 (用户原话"暂停命中后恢复刀刀震动")。参数链仿 §9 先例:
      config.vibration.storm_thr/storm_keep_pct/storm_win_ms/storm_pause_ms
      (serde default) → AppState 四原子 → run 循环每帧同步 → 引擎; GUI 在
      「群怪不打手」卡"命中风暴抽样"小节四滑块; 诊断行 [STORM] 开启/停顿恢复。
      新方案 (!legacy) 不进此分支 (红线); 回归测试 5 个 (抽样丢弃+锚点/记账零痕迹/
      停顿恢复/关闭态旧行为/受伤类排除/新方案红线)。
    - **v16.6 风暴静音怪物异常 (可选开关, 用户追加需求)**: 「命中风暴抽样」小节新增
      复选框"风暴期间暂时关闭怪物异常反馈 (出血/中毒跳字)"。勾选后风暴期 0x04
      零痕迹丢弃 (不注入/不打锚/不计密度/不进记账), 恢复用**时间窗判定**
      (`now - storm_last_hit <= storm_pause_ms`, 停手即恢复, 无需等下一发命中);
      玩家 DOT 0x20/状态 0x08/特殊 0x10 不受影响; **默认 false = 旧行为** (规范
      §9.3)。参数链 config.vibration.storm_mute_abnormal (bool serde default)
      → AppState AtomicBool → run 同步 → 引擎; 每个风暴期首条静音记一次
      `[STORM] 风暴期静音怪物异常反馈`; 回归测试 2 个 (开启零痕迹+DOT 不受影响+
      停顿恢复 / 关闭态照常)。全量 494/0。
    - **v16.1 零强度事件零影响 (审计发现的 0 值副作用)**: ① legacy 分通道戳从
      "间隔门后"再后移到 **s>0 门后** —— 怪物异常=0 时的出血洪流不再刷新注入窗
      (否则紧随其后的真实 DOT/状态/特效被挤进记账补发 = FLUSH 节拍感);
      S4 全局戳原位不动 (红线)。② 0 强度 DOT/状态/特效事件不再计入密度统计
      (font_pick 强度即注入强度, 单一事实来源)。测试: 0x04 洪水零痕迹 + 对照组。
    - **信息架构重组 v16.3→v16.5 (用户"全局系统和多数配置组合在一起难控难调难感知",
      布局经用户两轮定稿)**: 首版 (v16.3) 把群怪/连击/移动/输出质感提升为顶层卡片,
      用户实测否决 ("布局不行"), 定稿 v16.5 = **总阀只留常用全局旋钮, 高级算法与
      细调全部收进高级折叠区**。最终顺序: 控制中心 → (召唤专属) → 马达区 →
      **全局总阀 (总调整/攻击频率/强度上限/连击增强 + 随机性 + 移动持续震动强度/
      独立开关/L-R; 衰减时间 p5 预留死槽迁高级区)** → 伤害飘字 (开关文案修正:
      原"默认关闭"与实际相反; hint 改为"只管伤害飘字, 评分/移动不受影响") →
      评分特效 → 评分动态衰减 → **高级震动调校** (折叠; 勾"允许高级调校"后:
      **群怪不打手(S1, 13 滑块四小节) → 连击与自适应 (上限/斜率/密度/命中自适应,
      连击增强行已回总阀) → 走路质感与走位能量 (A2/A3, 移动强度已回总阀) →
      输出质感 (波形/平滑/节奏感/马达分工/衰减+衰减 L/R/S4 管线) → 算法窗口 C →
      静默脉冲 E → 预留槽位 p5**)。总阀卡攻击频率/强度上限行的死 L/R 不再渲染
      (引擎无消费点; item0 左右总闸保留)。离屏截图逐节验证 (总阀卡/高级区四小节/
      职业页/S4 页; 构建副本临时改初始页+滚动偏移 env 注入, 源码不动)。
    - **60 槽滑块自动落盘 v16.2**: 所有 60 槽滑块 change 分支在非职业模式下
      `sync_vib_config_from_params` 镜像进 config + 置 `vib_params_dirty_since`,
      主循环 (main_window update) 静默 1 秒后统一写 Vibration.toml —— 修"拖动
      滑块不点保存 → 重启回滚"高频痛点; 职业模式维持 JobVibration 快照链不变。
      页面提示同步改为"拖动即生效, 松手约 1 秒后自动保存"。
    - **小修批**: ① P_DEC_EFFECT 死常量删除 (params[39] 冲突 P1 结案, 无需 schema
      扩容); ② E/M/F 算法组 algo_state[0] 补 `== 0.0` 显式开窗守卫 (规范 §7.9);
      ③ VIB_RING_INVALID 只在 ring_ok→invalid 跳变记一条 (DLL 重启期不再刷
      crash.log; 环校验逻辑本身经审计确认正确, 8 条 = 8 次游戏重启)。
    - **ACT1 特供参数收敛 v16.4**: 命中 70→65 / 特殊 60→55 / 受击 45→40 (按剂量
      归因的三个最响通道各降 7-11%; 单挑手感基本不变, 高攻职业连打更耐听)。
      用户若不合意可随时用滑块微调 (现已自动保存) 或重新应用预设。
    - **验证**: 全量 **489/0** (lib 211 + bin 236 + 集成 42; 新增风暴 5 测试 +
      0 强度零影响 1 测试 + roundtrip 夹具 4 字段); 字节验证 命中风暴/群怪不打手/
      连击与自适应/移动与走路/输出质感/停顿恢复/自动保存 均在交付 exe 内;
      exe 已交付 (00:54, md5 d603c9b5...), 旧版备份 release_backup/。
    - **测试坑 (记录)**: ① 拨锚断言的锚点必须在拨锚之后取样 (拨锚本身会改写
      被断言的字段); ② carry_engine 不设 p[P_FONT_HIT], 受击类测试要显式给强度;
      ③ PowerShell `-File` 模式下类型字面量解析早于 Add-Type → System.Drawing
      需用 `-Command "Add-Type ...; & script.ps1"` 预加载; ④ PrintWindow 截图
      窗口高度受屏幕钳制, 滚动位置用构建副本临时 env 注入验证;
      ⑤ **断言要直接比对计数器, 别用可达性差的派生量** —— red-light 验证时
      "density_active==false" 在阈值 45 下用 10 条事件永远为绿 (弱断言),
      改为 density_hits==0 才红灯。
    - **复盘补记 (09-10 凌晨, diagnosing-bugs 纪律自查)**: ① **v16.2b 退出前
      强制落盘** —— 去抖落盘在 `should_exit` 早退之后, 拖完滑块 1 秒内关软件
      会丢最后一次改动; 修复 = 退出分支内强制写盘 (不等去抖窗口)。② **red-capable
      验证**: 在构建副本临时还原两处修复, 回归测试分别红灯 (注入窗戳断言 / 密度
      计数断言), 证明测试确实能抓住对应 BUG 而非碰巧通过。③ **全职业预设页与
     S4 页补验证**: 职业页共用卡片/事件网格/每项 L/R 渲染正常; S4 页群怪卡不
     渲染 (页面高度差约 1000px 佐证), 预设清单正确隐藏 ACT1 特供。

32. **S1 输出引擎模式 + 优先级阶梯 + 合并降幅 v20.0 (2026-09-10 上午, 用户三问一轮落地)**:
    - **背景 (用户原话)**: "S1 里很多高级参数概念和意思都相近, 调控的参数太多了... 概念
      相近的要不要整合在一起? 然后给高级参数定义一下优先级, 比如去解决持续震动的理所当然
      优先级要高一点, 避免其他的数值压过" + "给 S1 增加 S4 算法特调, 但也要保证玩家不丢失
      震动... 最重要的还是可以让玩家自由调整"。拍板 = 引擎层合并(仅 S1)/三档输出模式/固定阶梯。
    - **参数重叠盘点 (代码证据)**: S1 高级区 77 个可交互控件 (群怪不打手 22); 至少 9 组概念
      重叠, 三组共用同一份状态 (merge/hitcap 共用 `font_carry_s[3]`/`merge_cap`/`merge_hold`;
      hitmerge 与 hitcap_win 同通道双窗; 统合衰减期架空抽样/限频/聚合)。死控件: `p[5]` 预留
      衰减、S1 上不可达的 `p[15]` 装备特效。明细见 `docs/S1参数整合与S4特调方案_v1.md`。
    - **① 优先级阶梯 (仅 S1)**: 新增 `VibEngine.hard_cut_l/r` (帧内, tick 开头清零)。统合
      衰减/脉冲落地/单帧脉冲归零马达时置位; 爆发保底条件由"仅排除统合衰减期"推广为
      `!(legacy && hard_cut_r)`; 输出平滑三处调用收敛为 `VibEngine::smooth_out()` —— 硬归零
      的马达直接复位平滑状态 (不让一阶低通把静音尾巴拖回来)。S4 `hard_cut_*` 恒 false =
      逐字节不变。测试 3 个 (落地后爆发保底不得抬回 / S4 恒 false / 平滑复位)。
    - **② 三档输出模式 `legacy_output_mode` (0 经典/1 S4 纯/2 S4+不丢)**: 仅 S1 生效;
      默认 0 = 经典管线逐字节不变。模式 2 = S4 输出整形 (迟滞死区 + 重映射在后) +
      `shape_carry_and_filter` (低于死区的能量转入后置携带, 攒到阈值释放成厚脉冲, 无输入时
      ×0.9 泄放, 上限 2×阈值) = "轻反馈攒厚再出, 不丢击"。参数链: config(serde default 0)
      → AppState AtomicU32 → run 循环 `.min(2)` 同步 → engine。GUI: 输出质感区
      「S1 输出引擎模式 (S4 特调)」三选一; 模式 1/2 下死区%/重映射开关/重映射下限% 对 S1 解锁。
      测试 3 个 (模式 1 丢弃 / 模式 2 携带 / S4 红线)。
    - **③ 合并降幅 (仅 S1)**: 命中自适应与持续压制原为相乘叠加 (过压), 改为取 `min` 只应用
      一次; S4 维持逐项相乘。测试 1 个 (取强者非相乘)。
    - **④ UI 渐进披露 (v20.0, 用户"参数太多"的直接解法)**: 高级区新增「显示专家参数
      (低频微调)」复选框 (`SorahkGui.vib_show_expert`, 会话内瞬态, 默认关)。关时隐藏
      低频滑块 (**值不变, 只是不显示**): 群怪不打手的合并保留/封顶/补发窗、限频窗、风暴
      统计窗、统合衰减 ms、震尾切断%; 连击与自适应的密度 统计时长/恢复窗/恢复快慢
      (p30/32/34) 与命中自适应 斜率/补门槛/恢复间隔/保底 (p48/49/52/53)。高层旋钮 (开关+
      触发+降幅+保底+静音开关) 始终可见。离屏截图两态验证。
    - **验证**: 全量 **535/0** (lib 230 + bin 255 + 集成 50); 离屏截图 (S1 模式 2 选中态 +
      S4 旋钮解锁 / 节流仍禁用; 专家参数关=隐藏 / 开=恢复); 规范新增 §十三; exe 已交付
      (v20.0, md5 85a6a994...; 旧版备份 `release_backup/SorahkDFO-新UI版_v19.7_20260910.exe`)。
    - **剩余**: ① 密度 `density_scale` 折叠进合并降幅 (会改 S1 手感, 需实机试听后再动);
      ② merge/hitcap/hitmerge 引擎路径合并 (行为保持型, 不减少旋钮); ③ 死控件清理 (需确认)。

33. **v20.1 回归修复: 新算法泄漏进经典模式被彻底封堵 (2026-09-10 上午, 用户实机反馈
    "ACT1 特调手感差距有点大, 不如上一个版本")**:
    - **根因 (三段泄漏)**: v20.0 的优先级阶梯 (`hard_cut` 抑制爆发保底 + 输出平滑复位) 与
      合并降幅 (自适应/持续压制取 min) 都只写成 `if self.legacy`, 而**经典模式 0 也是
      legacy** → 三处行为全部泄漏进默认路径:
      ① 群怪 Burst 期右马达保底被抑制 (少一层 body);
      ② 输出平滑在落地帧被硬复位 (尾巴被削, 更干);
      ③ 降幅由相乘改取 min (压得更轻 = 更震, 与"治持续震"方向相反)。
    - **修复**: 三处全部 gate 到 `legacy && legacy_output_mode != 0`:
      爆发保底在模式 0 恢复 v19.7 原条件 `!(legacy && storm_unified_enabled && storm_active)`;
      `smooth_out` 模式 0 恢复纯一阶低通; 合并降幅模式 0 恢复逐项相乘。
    - **验证**: 逐行审计 `git diff` 确认所有行为改动均已 gate (经典模式 0 与 HEAD 等价);
      新增模式边界金标测试 3 个 (`legacy_mode0_burst_floor_still_applies_after_tail_land` /
      `legacy_mode0_smooth_out_ignores_hard_cut` / `legacy_adapt_sustain_merge_only_in_s4_mode`
      的模式 0 分支); 模式 2 补能量守恒测试。全量 **541/0**。
    - **测试坑 (记录)**: ① 手工置 `state=Burst` 的测试必须设 `last_event`, 否则 tick 的
      空闲判定把状态打回 Idle → 爆发保底断言"假绿"; ② 爆发保底每帧把 right 抬回地板,
      若地板 ≥ 落地线则落地永不触发 —— 测试参数须让地板低于落地线
      (P_BURST_MIN=30 / P_FONT_ATTACK=65 / tail_land_pct=50) 才可达。
    - **UI**: 「S1 输出引擎模式 (S4 特调)」选择器上移到高级区顶部 (紧随「显示专家参数」),
      默认仍是「经典」; exe 已交付 (v20.1, md5 6aa6e7db...)。
    - **教训入规范**: 规范 §13.1/13.3 补"生效范围"条款 —— 经典模式 0 是 ACT1 特调手感基线,
      任何新算法不得泄漏进该路径; 新增 legacy 行为必须先问"经典模式会不会也走这里"。

